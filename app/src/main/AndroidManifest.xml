<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pospointofsale">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Display_bills"></activity>
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".activities.customers" />
        <activity android:name=".activities.orderStats" />
        <activity android:name=".activities.settings" />
        <activity android:name=".activities.bill" />
        <activity android:name=".activities.ordersActivity" />
        <activity android:name=".activities.Scanning" />
        <activity android:name=".activities.home_new" />
        <activity android:name=".activities.item_details" />
        <activity android:name=".activities.Add_items" />
        <activity android:name=".activities.add_new_subcat" />
        <activity android:name=".activities.items_list" />
        <activity android:name=".activities.sub_cat" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.add_cat_subcat" />
        <activity android:name=".activities.home_details" />
        <activity android:name=".activities.Sign_up" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>